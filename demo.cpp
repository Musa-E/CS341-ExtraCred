/* Function signature breakdown:
	  // hire - creates a function
          // yourself - name of the function (naming the function "main" gives compiler error)
	  // int - return type
*/
hire yourself int
    crime 2 // Increases the heat level

    // Equivalent of a print statement; "say" is the command, anything after is outputted
    say "Welcome to a Fizz Buzz program in back alley!\n"

    // Equivalent of a for loop, from 0 to the number given (30 in this case)
    trip i 30

	 // Equivalent of an If statement
         ask criminal i != 0

	    // If block
            ask criminal i%3 == 0
                say "Fizz "
            stop asking // Equivalent of a "break" statement in a loop

            // If block
            ask criminal i%5 == 0
                say "Buzz"
            stop asking // Equivalent of a "break" statement in a loop

  	   // If block
            ask criminal i%3 != 0 and i%5 != 0
                say i   // outputs i
            stop asking // Equivalent of a "break" statement in a loop

            say ", "
        stop asking // stops the loop
    stop tripping   // stops the loop

pay 0 // ends the function and determines return value
